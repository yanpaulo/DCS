@page
@model DesafioControlSmart.WebApp.Pages.DeviceEvents.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>DeviceEvent</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="DeviceEvent.Id" />
            <div class="form-group">
                <label asp-for="DeviceEvent.Description" class="control-label"></label>
                <input asp-for="DeviceEvent.Description" class="form-control" />
                <span asp-validation-for="DeviceEvent.Description" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="DeviceEvent.Success" /> @Html.DisplayNameFor(model => model.DeviceEvent.Success)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="DeviceEvent.Timestamp" class="control-label"></label>
                <input asp-for="DeviceEvent.Timestamp" class="form-control" />
                <span asp-validation-for="DeviceEvent.Timestamp" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
